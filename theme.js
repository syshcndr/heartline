import { StyleSheet } from 'react-native';

const theme = StyleSheet.create({
  background: {
    backgroundColor: '#FFFFFF', // Cream background
  },
  textPrimary: {
    color: '#FF0000', // Classic Red for primary texts
  },
  textSecondary: {
    color: '#FFC0CB', // Pink for secondary texts
  },
  highlight: {
    color: '#FFD700', // Gold for highlighted texts or components
  },
  // Add more styles as needed
});

export default theme;